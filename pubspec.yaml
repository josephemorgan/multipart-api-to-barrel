name: api_barrel_file_converter
description: Converts an API library generated by openapi-generator-cli using the dart generator from a multipart library to a library using a barrel file.
version: 1.0.0
repository: https://github.com/josephemorgan/api_barrel_file_converter

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  args: ^2.6.0
  path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
